#include <stdio.h>
#include "logging.h"  // Ensure this declares logging_info

int main(int argc, char **argv) {
    logging_info("ExynosTools starting (Xclipse 940 non-fallback)");
    // Your main logic here
    return 0;
}
